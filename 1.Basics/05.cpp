#include <bits/stdc++.h>
using namespace std;
// learn about arrays
int main () {
    int arr[5];
    cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4];

    cout << arr[3];
    return 0;
    arr[3] = 100;
    // arrays are mutable
    // only similar datatypes are allowed\

    // 2-D array

    int twoDarray[5][3];
    // consider it like a matrix of 5 rows and 3 columns

    twoDarray[1][3] = 95; // 1st row and 3rd column

}